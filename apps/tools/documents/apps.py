from django.apps import AppConfig

class DocumentsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.tools.documents'
    label = 'tools_documents'
    verbose_name = 'Document Library'
